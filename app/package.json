{
	"name": "app",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "bun build index.ts --outdir dist --target bun --sourcemap",
		"app:local": "bun run --env-file=.env.local index.ts",
		"app:docker": "docker compose up app -d --build",
		"app:docker:down": "docker compose down app",
		"docker": "docker compose up -d",
		"docker:build": "docker compose up -d --build",
		"docker:down": "docker compose down",
		"test": "bun test"
	},
	"devDependencies": {
		"@types/bun": "^1.3.1",
		"pino-pretty": "^13.1.2"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"mqtt": "^5.14.1",
		"pino": "^10.1.0"
	}
}
