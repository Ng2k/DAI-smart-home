{
	"name": "app",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "bun build index.ts --outdir dist --target bun --sourcemap",
		"app:local": "bun run --env-file=./envs/.local.env index.ts",
		"app:docker": "docker compose up app -d --build",
		"app:docker:down": "docker compose down app",
		"docker": "docker compose up -d",
		"docker:build": "docker compose up -d --build",
		"docker:down": "docker compose down",
		"test": "bun test"
	},
	"devDependencies": {
		"@types/bun": "^1.3.1",
		"@types/pg": "^8.16.0",
		"drizzle-kit": "^0.31.8",
		"pino-pretty": "^13.1.2",
		"tsx": "^4.21.0"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"drizzle-orm": "^0.45.1",
		"mqtt": "^5.14.1",
		"pg": "^8.16.3",
		"pino": "^10.1.0"
	}
}
